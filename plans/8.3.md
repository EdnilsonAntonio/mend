# Plan: 8.3 — Add a Troubleshooting section to the README

## Goal

After this task, the root `README.md` contains a `## Troubleshooting` section, placed
between the Quickstart and `## Repository layout`, that answers — as eight short
symptom → cause → exact-command entries — every stall actually hit during the
fresh-clone test, plus the two dashboard error panels a stranger can reach. Each entry
quotes the **message the system prints today** (post-8.1 and post-8.2), not the
historical wording. The section is reachable from where people actually are when they
get stuck: one cross-link at the end of Setup (after `npm run db:migrate`) and one in
Quickstart (after the four-command block). This task is documentation only — no
production code is written, no behaviour changes.

### Spec/reality mismatch the Builder must not "fix"

`spec/TASKS.md` line 360 says to check that `OPENAI_API_KEY` "isn't still the
`your-open-ai-key` placeholder". **That string does not exist anywhere in this
repository.** The real placeholder, verified in `.env.example` line 7, is:

```
OPENAI_API_KEY=sk-your-key-here
```

The plan therefore documents `sk-your-key-here` — documenting a placeholder that does
not exist would defeat the purpose of the entry. The Builder must write
`sk-your-key-here` and must **not** edit `spec/TASKS.md` to match. Whether to correct
that line in `spec/TASKS.md` is the human's call at the end of the ABR loop.

### Why entry 1 is not written literally

Task 8.1 fixed the "`db:migrate` exits with no output" defect at the source: the CLI now
always prints a headline, a `target:` line, a `cause:` line, and a `Hint:` line
(`db/connection-errors.ts`, `db/cli/migrate.cli.ts`). Writing a troubleshooting entry
that describes a bug which no longer exists would be documentation rot on day one. The
acceptance criterion is still satisfied literally — an entry whose heading is the
symptom "`npm run db:migrate` exits with no output" exists — but its body says the
silence was a defect that is fixed, tells the reader what they will see instead, and
routes them to the two entries that decode those messages. The fix command for that
entry is "your checkout predates the fix; update".

---

## Files

- `README.md` — **MODIFY** — three edits, all additive: (1) one cross-link line after
  the Setup section's `npm run db:migrate` block; (2) one cross-link line after the
  Quickstart's four-command block; (3) a new `## Troubleshooting` section inserted
  between the Quickstart's last paragraph and `## Repository layout`.

**No other file is created, modified, or deleted.** In particular do not touch
`agent/`, `runner/`, `dashboard/`, `tests/`, `app-under-test/`, `db/`, `scripts/`,
`metrics/`, `breakage/`, `.env.example`, `package.json`, `spec/TASKS.md`, or any other
`plans/*.md`. If an entry below looks wrong, report it as Blocked — do not change code
to make the README true.

---

## Implementation steps

All line numbers refer to `README.md` as it stands at the start of this task (261
lines, ending with the `## License` section). Apply the edits **bottom-up** (Step 3
first, then Step 2, then Step 1) if you are working by line number, or match on the
quoted anchor text if you are working by search — the anchors below are unique.

### Step 1 — Setup cross-link

Find this block, which ends the `## Setup` section (currently lines 164–168):

````markdown
Apply the database schema:

```bash
npm run db:migrate
```
````

Immediately after the closing fence of that `bash` block, and before the blank line
preceding `## Quickstart: see a heal happen`, insert exactly:

```markdown
If that command prints an error instead of `Applied N migrations …`, see
[Troubleshooting](#troubleshooting).
```

### Step 2 — Quickstart cross-link

Find the Quickstart's four-command block (currently lines 175–180), which ends with the
`npm run heal` line and its closing fence. Immediately after that closing fence, insert
exactly:

```markdown
Hit an issue? See [Troubleshooting](#troubleshooting) — it lists the exact
messages these commands print when something is missing, and the one command
that fixes each.
```

Do not change the command block itself, and do not remove the existing sentence
"Then, in another terminal:" that follows.

### Step 3 — the Troubleshooting section

Insert the block below **between** the Quickstart's final paragraph (currently lines
201–202, the one beginning "To have `npm run heal` open pull requests…") and the
`## Repository layout` heading (currently line 204). Separate it from both neighbours
with exactly one blank line.

Copy the text verbatim. Every quoted message, command, and placeholder in it was read
out of the current source; re-wording any of them breaks the entry's purpose, which is
that a reader can paste the message they see into their editor's find bar and land on
the right entry.

`````markdown
## Troubleshooting

Every entry below is something that actually went wrong during a cold-clone run of
this README. Symptom, cause, fix — in that order.

### `npm run db:migrate` exits with no output

**Cause:** A real defect — the migration CLI swallowed connection errors. It is fixed:
`db:migrate` now always prints a headline, the connection target, the underlying
cause, and a hint before exiting non-zero.

**Fix:** If you still get a silent exit, your checkout predates the fix. Update it:

```bash
git pull
npm install
```

The message it prints from then on is decoded by the next two entries.

### `npm run db:migrate` prints `Could not reach DATABASE_URL — is Postgres running and is the database created?`

**Cause:** Nothing is listening on the host and port in your `DATABASE_URL` — usually
Postgres simply isn't running.

**Fix:** Start the local container, then re-run the migration:

```bash
docker run --rm -d --name mend-pg -p 5433:5432 \
  -e POSTGRES_PASSWORD=mend \
  -e POSTGRES_DB=mend \
  postgres:16
npm run db:migrate
```

The `target:` line in that error shows the host, port, database, and user it actually
tried (never the password) — check them against `.env`.

### `npm run db:migrate` prints `Reached the Postgres server, but database "mend" does not exist.`

**Cause:** The server is up, but the database named at the end of `DATABASE_URL` was
never created.

**Fix:** Create it, then re-run the migration:

```bash
psql "postgres://postgres:mend@localhost:5433/postgres" -c 'CREATE DATABASE mend'
npm run db:migrate
```

### `npm run heal` prints `DATABASE_URL is not set; export it before running npm run heal`

**Cause:** There is no `.env` at the repository root, or its `DATABASE_URL` line is
empty. The npm scripts load `.env` for you — they cannot invent it.

**Fix:** Create `.env` from the template (skip the copy if you already have one), fill
in `DATABASE_URL`, then confirm it is being picked up:

```bash
cp .env.example .env
MEND_ENV_DEBUG=1 npm run db:migrate:status   # prints "with-env: applied N variable(s) …"
```

### `npm run heal` prints `OPENAI_API_KEY is not set; export it before running a heal`

**Cause:** Same as above — the `OPENAI_API_KEY` line in `.env` is empty.

**Fix:** Put a real key in `.env`:

```
OPENAI_API_KEY=sk-...
```

### Every heal attempt comes back `failed`, including scenarios that should heal

**Cause:** `OPENAI_API_KEY` is still the `.env.example` placeholder
(`sk-your-key-here`), or is otherwise invalid — every OpenAI call is rejected with a
401, so the agent never gets as far as investigating.

**Tell-tale:** every `[settled]` line reports `toolCalls=0`, and the attempt's detail
page shows stop reason `model-error` with an `OpenAI request failed: 401 …` message.

**Fix:** Replace the placeholder in `.env` with a real key, then re-run:

```bash
npm run heal
```

Scenario 5 (the genuinely removed element) is *expected* to come back `failed` — that
one is the project working as designed, not a configuration problem.

### The dashboard shows "Set `DATABASE_URL` in `dashboard/.env.local` … then reload."

**Cause:** The dashboard is a separate Next.js app and reads its own
`dashboard/.env.local`, not the repository root `.env`.

**Fix:** Create it from the template and fill in the same `DATABASE_URL`:

```bash
cp dashboard/.env.local.example dashboard/.env.local
```

`next dev` picks the new file up on its own — reload the page, no `export` and no
restart. (With `direnv` active, the dashboard inherits `DATABASE_URL` from your shell
and this file is optional.)

### The dashboard shows "The database is unreachable or has not been migrated."

**Cause:** `DATABASE_URL` is set, but the database cannot be reached or the schema was
never created.

**Fix:** Check what the database actually has, then migrate:

```bash
npm run db:migrate:status
npm run db:migrate
```

If `db:migrate` itself fails, the first three entries above decode its message.
`````

### Step 4 — verification the Builder must actually run

```bash
# Section exists exactly once, and is positioned between Quickstart and Repository layout.
grep -n '^## ' README.md

# The eight entry headings are present.
grep -c '^### ' README.md          # must be >= 8 more than before this task

# Both cross-links exist.
grep -c '](#troubleshooting)' README.md       # must be 2

# Verbatim strings that must survive copy-paste.
grep -F 'Could not reach DATABASE_URL — is Postgres running and is the database created?' README.md
grep -F 'DATABASE_URL is not set; export it before running npm run heal' README.md
grep -F 'OPENAI_API_KEY is not set; export it before running a heal' README.md
grep -F 'sk-your-key-here' README.md

# Nothing else changed.
git status --porcelain            # only README.md (plus untracked dashboard/AGENTS.md,
                                  # dashboard/CLAUDE.md, which existed before this task)
git diff --stat                   # only README.md, additions only
```

The em dash in the `Could not reach DATABASE_URL` headline is a single U+2014
character, not a hyphen — it must match `db/connection-errors.ts` byte for byte, which
is why the `grep -F` check above is mandatory rather than optional.

---

## Interfaces / types

None. This task adds no code, no types, no exports, no npm scripts, and no
dependencies.

The following strings are **quoted** by the new section and are the contract this
documentation depends on. The Builder must not change any of them; they are listed so
the Reviewer can confirm each quote is faithful to its source:

| Quoted string | Source of truth |
| --- | --- |
| `Could not reach DATABASE_URL — is Postgres running and is the database created?` | `db/connection-errors.ts` → `UNREACHABLE_HEADLINE` |
| `Reached the Postgres server, but database "mend" does not exist.` | `db/connection-errors.ts` → `database-missing` headline |
| `Applied N migrations …` | `db/cli/migrate.cli.ts` → `summaryLineExtended` |
| `DATABASE_URL is not set; export it before running npm run heal` | `runner/cli/heal-run.cli.ts:78` |
| `OPENAI_API_KEY is not set; export it before running a heal` | `agent/loop/openai-client.ts:20` |
| `sk-your-key-here` | `.env.example:7` |
| `with-env: applied N variable(s) …` | `scripts/with-env.mjs` (`MEND_ENV_DEBUG=1`) |
| `Set DATABASE_URL in dashboard/.env.local …, then reload.` | `dashboard/app/_components/attempts-table.tsx:12` and `attempt-detail.tsx:25` |
| `The database is unreachable or has not been migrated.` | `dashboard/app/_components/attempts-table.tsx:13` and `attempt-detail.tsx:28` |
| `toolCalls=0`, `[settled]` | `runner/heal-run.ts:190` |
| `model-error`, `OpenAI request failed: 401 …` | `agent/loop/heal-loop.ts:149`, `agent/loop/openai-client.ts:51`; surfaced at `dashboard/app/_components/attempt-detail.tsx:405,407` |
| Docker command, `psql … CREATE DATABASE` target | `db/README.md` § Getting a database |

---

## Acceptance criteria

- [ ] `README.md` contains exactly one line matching `^## Troubleshooting$`.
- [ ] In the output of `grep -n '^## ' README.md`, `## Troubleshooting` appears after
      `## Quickstart: see a heal happen` and before `## Repository layout`.
- [ ] The Troubleshooting section contains exactly these eight `###` headings, in this
      order:
      1. `### \`npm run db:migrate\` exits with no output`
      2. `### \`npm run db:migrate\` prints \`Could not reach DATABASE_URL — is Postgres running and is the database created?\``
      3. `### \`npm run db:migrate\` prints \`Reached the Postgres server, but database "mend" does not exist.\``
      4. `### \`npm run heal\` prints \`DATABASE_URL is not set; export it before running npm run heal\``
      5. `### \`npm run heal\` prints \`OPENAI_API_KEY is not set; export it before running a heal\``
      6. `### Every heal attempt comes back \`failed\`, including scenarios that should heal`
      7. `### The dashboard shows "Set \`DATABASE_URL\` in \`dashboard/.env.local\` … then reload."`
      8. `### The dashboard shows "The database is unreachable or has not been migrated."`
- [ ] Each of the eight entries contains a line beginning `**Cause:**` and a line
      beginning `**Fix:**`.
- [ ] Each of the eight entries contains at most **6** lines of prose outside its code
      fences (count lines, not sentences; blank lines and fenced blocks do not count).
- [ ] Entries 1, 2, 3, 4, 6, 7, 8 each contain at least one fenced code block; entry 5
      contains a fenced block holding `OPENAI_API_KEY=sk-...`.
- [ ] `grep -c '](#troubleshooting)' README.md` returns `2`.
- [ ] One of those two links appears between the `npm run db:migrate` fence that ends
      `## Setup` and the `## Quickstart: see a heal happen` heading.
- [ ] The other appears between the Quickstart's `npm run heal` command fence and the
      line `Then, in another terminal:`.
- [ ] `grep -F 'Could not reach DATABASE_URL — is Postgres running and is the database created?' README.md`
      matches (em dash intact, byte-identical to `UNREACHABLE_HEADLINE` in
      `db/connection-errors.ts`).
- [ ] `grep -F 'DATABASE_URL is not set; export it before running npm run heal' README.md`
      matches, byte-identical to `runner/cli/heal-run.cli.ts:78`.
- [ ] `grep -F 'OPENAI_API_KEY is not set; export it before running a heal' README.md`
      matches, byte-identical to `agent/loop/openai-client.ts:20`.
- [ ] `grep -F 'sk-your-key-here' README.md` matches, byte-identical to `.env.example:7`.
      `README.md` does **not** contain the string `your-open-ai-key`.
- [ ] `grep -F 'cp dashboard/.env.local.example dashboard/.env.local' README.md` matches.
- [ ] The Troubleshooting section states that scenario 5 / the genuinely-removed element
      is expected to come back `failed`.
- [ ] No status value outside `investigating`, `healed`, `needs_review`, `failed` and no
      confidence value outside `high`, `low`, `none` appears in the new section.
- [ ] The new section contains no instruction to re-run, retry, loop, weaken a test,
      skip a test, lower a confidence threshold, or bypass verification.
- [ ] `git diff --stat` shows `README.md` as the only changed file, and the diff contains
      **zero** deleted lines (the change is purely additive).
- [ ] `git status --porcelain` shows no new untracked file created by this task.
- [ ] `npm run typecheck` exits 0 (unchanged — proves nothing was broken).

---

## Out of scope for this task

- **Any change to `spec/TASKS.md`**, including marking 8.3 done and including correcting
  the `your-open-ai-key` wording. Task status is the human's call at the end of the loop.
- **Any change to production code.** If a quoted message in the plan does not match the
  source, report it as Blocked; do not edit the source to match the README, and do not
  edit the README's quote to match a different source than the one in the table above.
- **Validating the `OPENAI_API_KEY` placeholder at runtime** (e.g. making `npm run heal`
  refuse `sk-your-key-here`). That was explicitly deferred to documentation in
  `plans/8.2.md`; turning it into code is a new task, not this one.
- **`dashboard/README.md`, `db/README.md`, `runner/README.md`, `agent/loop/README.md`.**
  No cross-links added to or from them.
- **Restructuring, shortening, or re-wording any existing README section**, including
  Setup, Quickstart, Prerequisites, Project status, and the badges. Additive edits only.
- **A table of contents, collapsible `<details>` blocks, emoji, or badges** for the new
  section.
- **Troubleshooting entries beyond the eight specified** — no "port 3200 already in
  use", no "Playwright browsers not installed", no "results.json not found", no npm/Node
  version entries. Those were not hit in the fresh-clone run; speculative entries are how
  troubleshooting sections rot.
- **Windows-specific instructions.**

---

## Risks

- **Em-dash corruption.** `Could not reach DATABASE_URL — …` uses U+2014. An editor or a
  copy through a terminal can silently substitute a hyphen, which makes the entry
  unfindable by the reader who greps their error message. The `grep -F` check in Step 4
  is the guard; if it fails, re-copy the string from `db/connection-errors.ts` line 12
  rather than retyping it.
- **Anchor link drift.** `[Troubleshooting](#troubleshooting)` depends on the heading
  being exactly `## Troubleshooting`. If the Builder renames the heading (e.g. adds
  "Common issues"), both links break silently. Do not rename it.
- **Insertion in the wrong place.** The Quickstart ends with the `GITHUB_TOKEN`
  paragraph, not with a code fence — it is easy to insert the new section one paragraph
  too early and orphan that sentence. Verify with `grep -n '^## ' README.md` that
  `## Troubleshooting` sits directly between the Quickstart and `## Repository layout`,
  and read the three lines above the new heading to confirm the `GITHUB_TOKEN` paragraph
  is still intact and still inside Quickstart.
- **`cp .env.example .env` can clobber a populated `.env`.** The entry-4 text says "skip
  the copy if you already have one" for exactly this reason. Do not drop that clause to
  make the entry shorter.
- **The 401 tell-tale is inferred from code, not observed.** A placeholder key produces
  `OpenAI request failed: 401 …` (`agent/loop/openai-client.ts:51`), which surfaces as
  `stopReason = 'model-error'` (`agent/loop/heal-loop.ts:149`) and `toolCalls=0` on the
  `[settled]` line. If the Builder happens to have a way to observe this and sees
  different output, report the discrepancy as a Deviation rather than guessing new
  wording.
- **Scope creep pressure.** Reading the README while editing it will surface other things
  worth improving. Do not act on any of them in this task.
